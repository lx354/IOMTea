# 先看我 — 项目索引与说明

欢迎。这个仓库是一个面向中度认知障碍老人居家监护的数字孪生系统，覆盖 Web 管理端和树莓派硬件终端。以下是快速了解和使用它的方式。

---

## 这个系统能做什么

```text
传感器/模拟数据 → 后端接入 → 状态评估 → Web 看板展示
                         ↕
                   告警推送 · ML 数据接口
```

- **风险监测看板**：Web 端以彩色矩阵展示每位老人的 11 维健康状态（含心率、血氧、夜间离床、重复行为、走失风险等）
- **数字孪生引擎**：内置 5 种患者画像，可模拟体征数据并实时评估综合风险等级（低风险/关注/告警/紧急）
- **告警与干预**：异常行为触发分级告警，附照护建议
- **ML 数据就绪**：时序数据导出接口已就绪，可直接用于 LSTM 模型训练
- **传感器接入**：支持树莓派 + DHT22 + PIR + 摄像头（MQTT 协议）

---

## 文档导航

| 先读这个 | 用途 |
|---------|------|
| [docs/getting-started.md](docs/getting-started.md) | 部署运行完整步骤，从零到跑通 |
| [docs/environment-setup.md](docs/environment-setup.md) | VS Code / Node.js / PostgreSQL 安装指南 |

| 需要时查阅 | 用途 |
|-----------|------|
| [docs/thesis-alignment.md](docs/thesis-alignment.md) | 系统架构与开题报告的四层映射（论文用） |
| [docs/CODE_MAP.md](docs/CODE_MAP.md) | 项目文件结构与关键代码位置 |

| 硬件相关 | 用途 |
|---------|------|
| [hardware/raspberry-pi/README.md](hardware/raspberry-pi/README.md) | 树莓派采购清单、接线图、Python 脚本 |

---

## 快速启动

完整的分步指南（含验证检查点）见 [docs/getting-started.md](docs/getting-started.md)，以下是精简版：

```bash
# 1. 安装 Node.js v24.14.0 + pnpm + Git（见 Step 1-3）
# 2. 启动数据库
docker run -d --name iomtea-pg -e POSTGRES_DB=iomtea -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:16-alpine

# 3. 克隆、安装、启动
git clone <仓库地址> iomtea && cd iomtea
pnpm install
cp .env.example .env
pnpm --filter @iomtea/server db:migrate
pnpm dev
```

浏览器打开 `http://localhost:5173`，使用 `demo / demo123` 登录。

> 详细的环境搭建见 [docs/environment-setup.md](docs/environment-setup.md)。每一步的验证检查点见 [docs/getting-started.md](docs/getting-started.md)。

---

## 项目结构速览

```
apps/server/src/
  index.ts          ← 服务端入口，启动流程（数据库→种子数据→HTTP→WebSocket）
  routes/           ← REST API 路由（15 个文件，约 70 个端点）
  modules/twin/     ← 数字孪生引擎（状态评估、模拟调度、场景注入）
  mqtt-ingest/      ← MQTT 设备数据接入（PIN 认证、指标归一化）
  core/db/          ← 数据库 Schema 定义（Drizzle ORM）

apps/web/src/
  pages/            ← 前端页面（TwinStatusMatrix 风险看板、告警管理等）
  hooks/            ← React Hooks（useTwinStatus 等）
  api/client.ts     ← 后端 API 调用封装

hardware/raspberry-pi/
  main.py           ← 树莓派主循环（20 秒一轮采集）
  sensors.py        ← DHT22 温湿度 + PIR 人体感应
  yolo.py           ← YOLOv8 跌倒检测
  mqtt_client.py    ← MQTT 数据发布
```

详见 [docs/CODE_MAP.md](docs/CODE_MAP.md)。

---

## 关键命令

```bash
pnpm dev                              # 启动全部
pnpm --filter @iomtea/server dev      # 仅启动后端
pnpm --filter @iomtea/web dev         # 仅启动前端
cd apps/server && pnpm db:migrate     # 数据库迁移
pnpm typecheck                        # 类型检查
```

---

## 后续建议

1. **如果需要演示**：直接运行 `pnpm dev`，内置孪生模拟器会自动生成数据，不需要硬件
2. **如果需要接入传感器**：参考 `hardware/raspberry-pi/README.md` 采购和接线
3. **如果补 LSTM 模型**：`ml-*` API 已提供时序数据导出和特征计算接口，数据管道就绪
4. **如果写论文**：[docs/thesis-alignment.md](docs/thesis-alignment.md) 逐条对应了开题报告的四层架构
